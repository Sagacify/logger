dependencies:
  override:
    - sudo pip install codecov

test:
  override:
    - npm test
    - codecov

deployment:
  npm:
    branch: master
    commands:
      - echo -e "$NPM_USERNAME\n$NPM_PASSWORD\n$NPM_EMAIL" | npm login
      - npm run 2npm
